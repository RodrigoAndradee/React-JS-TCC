import styled from "styled-components";

import { Row } from "antd";

export const StyledToolbar = styled(Row)`
  padding: 10px;
  width: 100%;

  .select-category {
    text-align: left;
    width: 100%;
  }

  .add-product-button {
    float: right;
  }
`;
